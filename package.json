{
  "name": "cmc-app-deploy",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install --legacy-peer-deps && npm run build && cd ../server && npm install",
    "start": "cd server && node server.js"
  }
}